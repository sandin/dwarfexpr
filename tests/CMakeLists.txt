if (${LIBDWARF_ENABLED})
    add_subdirectory(dwarfexpr)
endif()
if (${BREAKPAD_ENABLED})
    add_subdirectory(breakpad_demo)
endif()
if (${MINIDUMP_ENABLED})
    add_subdirectory(minidump_dump)
endif()